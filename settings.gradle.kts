plugins {
    // Lets Gradle download the Java 25 toolchain when the machine doesn't have one.
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.10.0"
}

rootProject.name = "DripleafRebirth"
